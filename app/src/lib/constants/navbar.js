/** Shared circular action button in the top navbar (cart, notifications, menu). */
export const NAVBAR_ICON_BTN_CLASS =
  'flex h-9 w-9 shrink-0 items-center justify-center rounded-full bg-[#141412] transition-colors hover:bg-zinc-700 active:bg-zinc-600';

/** Badge overlay for navbar icon buttons (unread notifications, cart item count). */
export const NAVBAR_ICON_BADGE_CLASS =
  'absolute -top-0.5 -right-0.5 flex min-h-[16px] min-w-[16px] items-center justify-center rounded-full bg-[#DA9811] px-1 text-[10px] leading-none font-bold text-[#080807]';
