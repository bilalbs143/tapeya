/** Debounce delay (ms) for search input before triggering API. */
export const DEBOUNCE_MS = 300;

/** Minimum character length before showing search results or triggering search. */
export const MIN_SEARCH_LENGTH = 2;

/** Maximum number of search results shown in popover dropdowns. */
export const SEARCH_RESULTS_LIMIT = 8;
