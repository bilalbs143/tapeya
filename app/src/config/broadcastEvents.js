/**
 * Echo private-channel event names (leading dot). Keep in sync with
 * `api/app/Support/Broadcast/BroadcastEventNames.php` (user.* only).
 */
export const USER_APP_BROADCAST_EVENTS = ['.user.order.placed', '.user.order.status_updated'];
