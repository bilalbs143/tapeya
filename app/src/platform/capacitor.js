/**
 * Capacitor plugin helpers
 * Install plugins as needed: @capacitor/status-bar, @capacitor/keyboard, etc.
 * Then add lazy loaders here to keep web bundle small
 *
 * Example:
 * export const getStatusBar = () => import('@capacitor/status-bar').then(m => m.StatusBar);
 */
