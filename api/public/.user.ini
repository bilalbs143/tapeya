; PHP-FPM / Herd / Valet read this from the public docroot.
; App reel validation has no size/duration cap — raise PHP so uploads aren't dropped first.
upload_max_filesize = 10G
post_max_size = 10G
max_execution_time = 0
max_input_time = 0
